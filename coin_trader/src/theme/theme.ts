import {Theme} from '@theme/theme.type';

import {
  colors,
  fonts,
  fontSizes,
  units,
} from '@theme/theme.values';

const theme: Theme = {
  colors,

  fonts,
  fontSizes,

  units,
};

export default theme;
